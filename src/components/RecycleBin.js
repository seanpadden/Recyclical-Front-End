import React, { Component, Fragment } from 'react';

export default class RecycleBin extends Component {
  render () {
    return (
      <div>
        {/* {this.props.bin.borough === "Brooklyn" ? this.props.bin.address : null} */}
      
      </div>
    )
  }
}